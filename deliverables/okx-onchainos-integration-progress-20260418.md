# 海豚社区 App 对接 OKX Onchain OS 进展说明（2026-04-18）

**作者：Manus AI**

本轮工作继续严格遵循一个前提：**海豚社区只做 API 对接、能力编排与产品封装，不重复开发 OKX 已经完成的底层钱包、交易、行情、赚币与安全能力**。在这个原则下，我已把本轮首批落地的对接重点收敛到三条主线：其一，是把 **Builder Code / `co`** 兼容读入现有配置链路；其二，是把 **钱包安全授权查询** 作为 OKX 已封装能力正式并入钱包技能；其三，是把 **行情页热门代币列表** 从占位能力改为走 OKX 官方 token ranking 能力的真实入口。

## 一、本轮已完成的实际接入范围

| 技能域 | 本轮接入内容 | 当前结果 |
|---|---|---|
| **钱包** | 补齐 OKX 授权查询 approvals 能力，纳入统一链上服务层、serverless 入口、Express 入口与前端 API 封装 | 已完成 |
| **钱包** | 钱包页新增“授权与风险暴露摘要”卡片，加载真实 approvals 数据并形成前端摘要展示 | 已完成 |
| **交易** | 统一链上服务继续保持与真实 OKX 交易主链一致，同时补齐 Builder Code 配置状态识别 | 已完成 |
| **行情** | 为 `token_hot_tokens` 新增本地别名并接到 OKX 官方 token ranking 工具 | 已完成 |
| **赚币** | 维持既有 DeFi 搜索能力接入，不再重复开发底层逻辑 | 已完成并保留 |
| **配置归因** | 支持从 `EXPO_PUBLIC_CO` / `EXPO_PUBLIC_OKX_CO` 读取用户提供的 Builder Code（`yf83qce657mgxsjw`） | 已完成 |

从工程角度看，本轮关键文件共涉及 **8 个核心文件**，累计变更规模约为 **443 行新增、5 行删除**，已覆盖配置层、服务层、双入口路由层、前端 API 层与钱包页展示层。

## 二、本轮关键改动文件

| 文件 | 作用 | 本轮变化 |
|---|---|---|
| `lib/builder-code.ts` | Builder Code 统一读取 | 增加 `co` 变量名兼容读取 |
| `.env.example` | 环境配置示例 | 明确支持 `EXPO_PUBLIC_CO` / `EXPO_PUBLIC_OKX_CO` |
| `server/_core/onchain-os.ts` | 统一链上服务抽象 | 新增 approvals 查询与 Builder Code 配置状态修正 |
| `api/onchain/index.ts` | Serverless 链上入口 | 补齐 approvals 路由 |
| `server/_core/onchain-os-routes.ts` | Express 链上入口 | 同步补齐 approvals 路由 |
| `lib/_core/api.ts` | 前端统一 API 封装 | 增加 approvals 类型、请求函数与 capability 暴露 |
| `api/okx/mcp.ts` | OKX MCP 桥接 | 新增 `token_hot_tokens`，映射到 OKX token ranking 官方工具 |
| `app/(tabs)/wallet.tsx` | 钱包页 | 接入 approvals 摘要卡片并展示真实授权风险暴露信息 |

## 三、本轮验证结果

本轮改动后，我已再次执行 TypeScript 全量校验，当前仓库可以通过：

> `pnpm -s tsc --noEmit`

这意味着本轮新增的 **Builder Code 兼容、approvals 服务与路由、前端钱包安全摘要、热门代币桥接入口** 没有引入新的类型错误或语法错误，当前代码已经具备进入逐项业务验证的基础状态。

## 四、现在可以开始的测试顺序

接下来不建议一上来做大范围联测，而应按你说的方式，**一个一个来测**。最稳的顺序如下。

| 优先级 | 测试项 | 入口 | 预期结果 |
|---|---|---|---|
| **P0** | 邮箱注册/验证后生成 Agent Wallet | 登录/注册主链 | 正常生成钱包，前端可拿到 EVM / Solana 地址 |
| **P0** | 钱包资产读取 | 钱包页 | 能返回真实链上余额与多链资产 |
| **P0** | Wallet approvals 授权查询 | 钱包页安全卡片 / `/api/onchain` | 能返回 OKX 授权数据；无授权时为空列表 |
| **P0** | Swap 报价与执行预览 | Chat / 交易入口 | 返回真实报价与执行参数，不再走 mock |
| **P1** | 热门代币列表 | 行情页 | 能返回 OKX token ranking 列表并正常渲染 |
| **P1** | 代币价格快照 | 行情页 / 聊天入口 | 返回实时价格、涨跌幅与成交量 |
| **P1** | DeFi 搜索 | Earn 页 | 能查到真实赚币产品并正确映射收益字段 |
| **P2** | Builder Code 归因校验 | 交易请求链路 | `co=yf83qce657mgxsjw` 被正确注入当前链路 |

## 五、当前仍建议继续补齐的事项

虽然本轮已经把钱包安全与行情热榜补到统一链路里，但从“首轮全量对接”的目标来看，仍建议继续沿以下方向推进。

| 后续项 | 说明 |
|---|---|
| **继续扩充钱包安全能力** | 当前已接入 approvals 查询，下一步可继续把恶意授权撤销、风险提示等 OKX 已封装能力接进统一链路 |
| **把行情页更多入口统一收口** | 当前热榜已接真实接口，后续可继续把搜索、榜单、聪明钱等入口完全归入四技能统一口径 |
| **把赚币页策略层与官方产品层分离** | 官方 API 负责产品搜索与数据，海豚社区只保留策略推荐、提醒与任务编排 |
| **继续做真实链路验证而非静态检查** | 编译通过只是第一步，后续必须逐项走真实请求验证返回数据是否符合页面消费预期 |

## 六、结论

截至目前，海豚社区 App 已经从“理解 OKX 能力”阶段，进入到**实际对接 OKX Onchain OS 四大技能**的执行阶段，而且已经有一批核心能力完成首轮落地。尤其是 **Builder Code 配置兼容、钱包 approvals 安全能力接入、热门代币真实排行入口接通、钱包页前端展示联动** 这几项，标志着项目已经不再停留在方案设计层，而是在按既定边界逐步把 OKX 已经开发好的能力接进来。

接下来最适合的方式，不是继续扩展讨论，而是按照上面的 **P0 → P1 → P2** 顺序，和你一起逐项实测。
